// CheckTests removed: the Check class is `internal` in Microsoft.EntityFrameworkCore.Utilities
// and cannot be tested directly without InternalsVisibleTo + a public key.
// Its logic is covered indirectly via the public APIs that use it.
